const createProductService = (input) => new Promise(async (resolve, reject) => {
    try {
        //xu ly tao product

        return resolve()//them data sau khi xu ly o buoc tren vao
    } catch (err) {
        return reject(err)
    }
})

module.exports = {
    createProductService
}